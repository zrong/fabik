---
description: 项目依赖管理规范
---
# 依赖管理规范

对于 `fabik` 项目的依赖管理：

1. 所有项目依赖应在 [pyproject.toml](mdc:pyproject.toml) 的 `dependencies` 列表中声明
2. 开发依赖应放在 `[project.optional-dependencies]` 的 `dev` 列表中
3. 使用明确的版本约束来避免依赖冲突
4. 安装依赖时使用虚拟环境
5. 定期更新依赖以修复安全漏洞
